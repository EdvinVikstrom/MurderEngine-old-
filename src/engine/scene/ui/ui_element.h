#ifndef UI_ELEMENT_H
  #define UI_ELEMENT_H

#include "../../math/transform.h"
#include "../scene.h"

namespace me {

  namespace ui {
/*
    struct ui_element : me::item {

      ui_element(std::string identifier, me::transform transform, me::wcolor color) : item(identifier, me::item_type::SPRITE, transform, color.image)
      {
      }

      virtual bool onMouseInput(int action, double posX, double posY, int button) = 0;
      virtual bool onKeyInput(int action, int key) = 0;

    };
*/
  };

};

#endif
